# Functional test constraints for charm tox func-* environments.
# setuptools>=82 removes pkg_resources, which is still imported by
# python-keystoneclient via zaza-openstack utilities in current test paths.
setuptools<82
